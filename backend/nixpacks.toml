# Désactiver la détection automatique des variables d'environnement
[phases.setup]
nixPkgs = ["nodejs_22"]
# Pas de variables ENV dans le setup

[phases.install]
cmds = ["npm install"]

[start]
cmd = "npm start"
